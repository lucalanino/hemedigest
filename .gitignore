__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
*.bundle
.venv

legacy-code/

# Local config overlay with real Azure values (endpoint/deployment/tenant) — never commit
config.local.yaml

# Patient data (PHI) and run outputs — never commit
data/